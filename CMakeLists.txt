PROJECT(heads_live_deployments)
cmake_minimum_required(VERSION 2.6)

SET (CMAKE_MODULE_PATH "${CMAKE_CURRENT_SOURCE_DIR}/.orogen/config")
INCLUDE(heads_live_deploymentsBase)

# FIND_PACKAGE(KDL)
# FIND_PACKAGE(OCL)

